# Postgres (relational: users, classes, questions, tests, submissions)
DATABASE_URL=postgresql+asyncpg://gary:gary@localhost:5432/gary

# MongoDB (knowledge graph: nodes, edges, per-student mastery state)
MONGO_URL=mongodb://gary:gary@localhost:27017/?authSource=admin
MONGO_DB=gary

# Auth
JWT_SECRET=change-me-in-prod
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173

# LLM (OpenAI-compatible, used by ingestion tagging + learning-path agent)
OPENAI_API_KEY=sk-placeholder
OPENAI_BASE_URL=
GARY_LLM_MODEL=gpt-4o
