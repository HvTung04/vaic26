import numpy as np


def read_bubble_sheet(image: np.ndarray) -> dict[str, str]:
    """
    Đọc bubble sheet bằng OpenCV template cố định (không OCR chữ viết tay).
    Trả về {question_number: selected_option}.
    TODO: BE/AI 1 — xem plan.md § 0 (bubble sheet đọc bằng OpenCV template cố định).
    """
    raise NotImplementedError
